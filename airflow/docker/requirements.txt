# dbt for the container image (runs via KubernetesPodOperator on Composer; no Airflow here, so this
# resolves cleanly on a plain python base, with none of the airflow<->dbt conflicts).
dbt-core>=1.10,<1.11
dbt-bigquery>=1.10,<1.11
dbt-duckdb>=1.10,<1.11

# Lineage. dbt-ol wraps dbt and reports each model's inputs, outputs and column lineage as
# OpenLineage events; the gcplineage extra sends them to Knowledge Catalog.
openlineage-dbt>=1.53,<2
openlineage-python[gcplineage]>=1.53,<2
