# Lean package set: dbt_utils + elementary (both broadly dbt-version compatible). dbt_expectations was
# dropped — it was unused and only dragged in dbt_date.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  - package: elementary-data/elementary
    version: [">=0.16.0", "<1.0.0"]
