# Dagster code location runtime. Kept in its own environment (image / venv), not alongside Airflow, so dbt
# and Dagster resolve cleanly. dagster-dbt's minor (0.2x) tracks the dagster 1.x minor.
dagster>=1.10,<1.13
dagster-webserver>=1.10,<1.13
dagster-dbt>=0.26,<0.29
dagster-postgres>=0.26,<0.29
dagster-slack>=0.26,<0.29

# dbt — same adapters/version as the production runner image (airflow/docker/requirements.txt).
dbt-core>=1.10,<1.11
dbt-duckdb>=1.10,<1.11
dbt-bigquery>=1.10,<1.11

# Used by the dev-only raw-data ingestion asset (scripts/generate_test_data.py).
duckdb>=1.1
pandas>=2,<3
numpy>=2,<3
